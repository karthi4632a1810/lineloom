# Copy to ".env" in this folder. Values usually match Web.config `BB_CONSTR` (KMCH_Frontoffice / sa).
# PHP loads .env via bootstrap.php. If omitted, config.php uses the same defaults as Web.config.

OP_IP_DB_SERVER=WIN-65URRV9USQE
OP_IP_DB_NAME=KMCH_Frontoffice
OP_IP_DB_USER=sa
OP_IP_DB_PASS=M@p1mssql
